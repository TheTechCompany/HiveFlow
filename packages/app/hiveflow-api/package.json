{
  "name": "@hive-flow/api",
  "version": "1.0.0",
  "description": "GraphQL operations for HiveFlow — uses @apollo/client",
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.7",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@apollo/client": "^3.13.8",
    "graphql": "^16.2.0"
  }
}
