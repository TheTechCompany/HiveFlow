// react-doc-viewer stub — not used directly, but @hexhive/ui has it as a dependency.
// Its internal react-pdf/pdfjs-dist chain crashes single-spa webpack.
module.exports = { default: () => null };
module.exports.DocViewer = () => null;
